数据与隐私说明
==============

1. 本软件采用本地优先架构，默认不把岗位数据库上传到云端。
2. FastAPI 服务仅绑定 127.0.0.1，供本机浏览器扩展和页面访问。
3. 岗位、个人档案、项目证据、人工校准和投递状态保存在：
   %LOCALAPPDATA%\JobMarketDecisionSystem
4. 软件发布包不应包含 job_market.db、api_token.txt 或真实岗位导出文件。
5. 浏览器扩展令牌仅用于本机 API 鉴权，不是招聘网站账号密码。
6. 删除软件目录不会自动删除用户数据；需要彻底删除时，请手动删除上述用户数据目录。
