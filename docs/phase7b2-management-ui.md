# Phase 7B.2：岗位管理中心

页面地址：`http://127.0.0.1:8765/manage`

已实现：求职状态标签、组合筛选、岗位卡片、多选批量操作、详情抽屉、个人状态、招聘状态、分析口径、人工分类管理标签、备注、归档恢复、状态历史和原岗位链接。

`quality_override=exclude` 已接入 SQLite 导出：岗位仍保存在数据库和管理中心，但下一次分析时不会进入输入 JSONL。

人工分类本阶段先用于管理筛选，覆盖统计图表分类留到后续分析覆盖层。

管理页面不会把 API 令牌嵌入 HTML。首次使用时手动粘贴，令牌只保存在当前浏览器 localStorage。
